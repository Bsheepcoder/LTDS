spring:
  cloud:
    nacos:
      username: nacos
      password: Casit.nacos
      config:
        server-addr: ${nacos.url}:8848
        namespace: ${nacos.namespace}
        group: DEFAULT_GROUP
        prefix: user-service
        file-extension: yaml
      discovery:
        server-addr: ${nacos.url}:8848
        namespace: ${nacos.namespace}


